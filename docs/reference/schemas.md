# Schemas

Authoritative Pydantic models live in the package. Generated JSON Schemas are
projections and carry drift checks; hand-authored web copies are not authoritative.

Use [DATA_DICTIONARY.md](../../DATA_DICTIONARY.md) for core prediction/report fields
and each contract README for its schema paths, generation command, budgets, and
versioning rules.

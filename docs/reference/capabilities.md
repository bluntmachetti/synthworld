# Capabilities

The [registry catalogue](/benchmarks/catalogue) is generated during documentation
preparation from `docs/_data/capabilities.resolved.json`. It is an explicitly
allowlisted public projection of code-derived facts and curated claims rather than
a hand-maintained status table or a copy of the raw registry.

Capability maturity does not imply benchmark publication, external availability, or
a production support promise.

{
  "name": "synthworld-docs",
  "version": "0.0.0",
  "private": true,
  "description": "Hermetic documentation build for SynthWorld.",
  "engines": {
    "node": "22.12.0",
    "npm": "10.9.0"
  },
  "packageManager": "npm@10.9.0",
  "scripts": {
    "docs:prepare": "node tools/prepare_docs_content.mjs && node tools/generate_registry_catalogue.mjs && node tools/normalize_blume_headings.mjs",
    "docs:build": "npm run docs:prepare && blume build",
    "docs:check": "npm run docs:prepare && blume check --strict && blume validate && blume build && blume audit --verbose && node tools/audit_docs_dist.mjs && node tools/audit_docs_headings.mjs",
    "docs:preview": "npm run docs:prepare && blume preview",
    "explorer:build": "node tools/build_explorer_assets.mjs --write",
    "explorer:check": "npm run explorer:test && node tools/build_explorer_assets.mjs --check",
    "explorer:test": "node --test tools/explorer/replay.test.mjs"
  },
  "dependencies": {
    "blume": "1.4.0"
  },
  "devDependencies": {
    "cytoscape": "3.34.1",
    "elkjs": "0.12.0",
    "esbuild": "0.28.2"
  }
}
